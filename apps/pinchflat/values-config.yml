---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
global:
  fullnameOverride: pinchflat

serviceAccount:
  pinchflat: {}

controllers:
  pinchflat:
    containers:
      app:
        image:
          repository: ghcr.io/communitymaintained/pinchflat
          tag: 1.3.0

defaultPodOptions:
  securityContext:
    runAsNonRoot: true
    runAsUser: 1000
    runAsGroup: 1000
    fsGroup: 1000
    fsGroupChangePolicy: OnRootMismatch

service:
  app:
    controller: pinchflat
    ports:
      http:
        port: 8945

ingress:
  app:
    className: nginx
    hosts:
      - host: pinchflat.cjf.sh
        paths:
          - path: /
            service:
              identifier: app
              port: http
    tls:
      - hosts:
          - pinchflat.cjf.sh

persistence:
  config:
    type: persistentVolumeClaim
    storageClass: zfs-dataset
    accessMode: ReadWriteOnce
    size: 32Mi
    globalMounts:
      - path: /config

  downloads:
    type: nfs
    path: /var/tank/media/yt
    server: 192.168.200.13
    globalMounts:
      - path: /downloads
