---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hass
  namespace: flux-system
spec:
  chartRef:
    kind: OCIRepository
    name: hass
    namespace: flux-system
  install:
    createNamespace: true
  interval: 1m0s
  targetNamespace: hass
  valuesFrom:
  - kind: ConfigMap
    name: hass-config-values
