---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: piper
  namespace: flux-system
spec:
  chartRef:
    kind: OCIRepository
    name: piper
    namespace: flux-system
  install:
    createNamespace: true
  interval: 1m0s
  targetNamespace: hass
  valuesFrom:
  - kind: ConfigMap
    name: piper-config-values
